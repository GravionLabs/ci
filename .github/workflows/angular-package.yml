name: Angular Package CI/CD

on:
  workflow_dispatch:
    inputs:
      force-publish:
        description: 'Publish regardless of the current branch'
        type: boolean
        default: false
  workflow_call:
    inputs:
      node-version:
        description: 'Node.js version to use (e.g. 20, 22, 24)'
        type: string
        required: false
        default: '24'
      package-manager:
        description: 'Package manager to use (npm, pnpm, yarn)'
        type: string
        required: false
        default: 'npm'
      pnpm-version:
        description: 'Version of pnpm to install. Leave empty to auto-detect from the packageManager field in package.json.'
        type: string
        required: false
        default: ''
      working-directory:
        description: 'Working directory containing the Angular workspace'
        type: string
        required: false
        default: '.'
      build-command:
        description: 'Override the build command. If empty, defaults to ng build.'
        type: string
        required: false
        default: 'ng build'
      run-tests:
        description: 'Whether to run the test suite after build'
        type: boolean
        required: false
        default: true
      test-command:
        description: 'Override the test command. If empty, defaults to ng test with ChromeHeadless and coverage.'
        type: string
        required: false
        default: 'ng test --no-watch --code-coverage --browsers=ChromeHeadless'
      coverage-format:
        description: 'Coverage report format (cobertura, lcov)'
        type: string
        required: false
        default: 'cobertura'
      gitversion-config-file:
        description: 'Path to a GitVersion.yml configuration file (optional)'
        type: string
        required: false
        default: ''
      publish-directory:
        description: 'Directory containing the built Angular package to publish (e.g. dist/my-lib). Passed to the publish action to construct the correct publish command.'
        type: string
        required: false
        default: ''
      publish-command:
        description: 'Override the publish command. If empty, auto-generated from package-manager and publish-directory.'
        type: string
        required: false
        default: ''
      run-lint:
        description: 'Whether to run the lint step before build'
        type: boolean
        required: false
        default: true
      lint-command:
        description: 'Override the lint command. If empty, defaults to ng lint.'
        type: string
        required: false
        default: 'ng lint'
      upload-build-artifact:
        description: 'Whether to upload the build output as an artifact for the publish job'
        type: boolean
        required: false
        default: true
      build-artifact-name:
        description: 'Name of the build artifact to upload'
        type: string
        required: false
        default: 'node-build'
      force-publish:
        description: 'Publish regardless of the current branch'
        type: boolean
        required: false
        default: false
      changelog-file:
        description: 'Path to a CHANGELOG.md file to use for release notes'
        type: string
        required: false
        default: 'CHANGELOG.md'
      cliff-config:
        description: 'Path to a cliff.toml file for git-cliff changelog generation (optional)'
        type: string
        required: false
        default: ''
      release-tag-prefix:
        description: 'Prefix for the release git tag (e.g. "v" → v1.2.3)'
        type: string
        required: false
        default: 'v'
    secrets:
      npm-token:
        description: 'NPM token or GitHub PAT for registry authentication'
        required: true

jobs:
  determine-version:
    name: Determine Version
    runs-on: ubuntu-latest
    outputs:
      semver: ${{ steps.version.outputs.semver }}
      npm-version: ${{ steps.version.outputs.npm-version }}
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Determine version
        id: version
        uses: GravionLabs/ci/versioning/determine-version@main
        with:
          gitversion-config-file: ${{ inputs.gitversion-config-file }}

  build-test:
    name: Build & Test
    runs-on: ubuntu-latest
    permissions:
      contents: read
      checks: write
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: CI
        uses: GravionLabs/ci/node/ci@main
        with:
          node-version: ${{ inputs.node-version }}
          package-manager: ${{ inputs.package-manager }}
          pnpm-version: ${{ inputs.pnpm-version }}
          working-directory: ${{ inputs.working-directory }}
          run-lint: ${{ inputs.run-lint }}
          lint-command: ${{ inputs.lint-command }}
          build-command: ${{ inputs.build-command }}
          run-tests: ${{ inputs.run-tests }}
          test-command: ${{ inputs.test-command }}
          coverage-format: ${{ inputs.coverage-format }}

      - name: Upload build artifact
        if: ${{ inputs.upload-build-artifact }}
        uses: actions/upload-artifact@v4
        with:
          name: ${{ inputs.build-artifact-name }}
          path: ${{ inputs.working-directory }}
          if-no-files-found: warn

  publish:
    name: Publish
    needs: [determine-version, build-test]
    if: github.ref == 'refs/heads/main' || inputs.force-publish == true
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - name: Publish
        uses: GravionLabs/ci/node/publish@main
        with:
          package-manager: ${{ inputs.package-manager }}
          npm-token: ${{ secrets.npm-token }}
          working-directory: ${{ inputs.working-directory }}
          publish-directory: ${{ inputs.publish-directory }}
          publish-command: ${{ inputs.publish-command }}
          download-artifact: ${{ inputs.upload-build-artifact }}
          artifact-name: ${{ inputs.build-artifact-name }}

  release:
    name: Release
    needs: [determine-version, publish]
    if: github.ref == 'refs/heads/main' || inputs.force-publish == true
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Create GitHub Release
        uses: GravionLabs/ci/versioning/gh-release@main
        with:
          version: ${{ needs.determine-version.outputs.semver }}
          tag-prefix: ${{ inputs.release-tag-prefix }}
          changelog-file: ${{ inputs.changelog-file }}
          cliff-config: ${{ inputs.cliff-config }}
